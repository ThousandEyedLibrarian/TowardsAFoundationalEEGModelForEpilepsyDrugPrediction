# Core dependencies for EEG analysis script
# Compatible with MNE 1.0+ and latest versions as of 2025
mne>=1.0.0
mne-connectivity>=0.5.0  # For connectivity analysis (optional but recommended)
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.4.0
seaborn>=0.11.0

# Optional but recommended
scikit-learn>=1.0.0  # For ICA and machine learning features
h5py>=3.0.0  # For some file formats
pymatreader>=0.0.24  # For MATLAB file support

# For Excel export (optional)
openpyxl>=3.0.0

# For parallel processing (optional but speeds up ICA)
joblib>=1.0.0
